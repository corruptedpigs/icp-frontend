# Local secrets — never committed to git (see .gitignore)
# Copy this file to .env.local and fill in the real values before running locally.

# Add any Next.js-only (server-side) secrets here.
# Do NOT add GAME_RESULT_API_URL or GAME_RESULT_API_KEY — those are stored in
# the Motoko canister and set post-deploy via setGameResultApiUrl / setGameResultApiKey.
